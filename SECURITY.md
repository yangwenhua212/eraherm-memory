# Security Policy

## 支持的版本

| 版本 | 支持 |
|------|------|
| 0.8.x | ✅ 当前 |
| < 0.8 | 有限（`0.x` 可能含破坏性变更） |

## 上报漏洞

请通过私有渠道联系维护者（GitHub Security Advisory / 私信），**不要**在公开 Issue 中披露可复现攻击细节。

请包含：影响版本、复现步骤、预期与实际行为、是否已在公网暴露。

## 已知信任边界（请务必阅读）

EraHerm-Memory 默认面向 **本地或可信内网**：

- `ERAHERM_ADMIN_TOKEN` 默认值为开发令牌，**上线前必须更换**
- 记忆读写 API **无完整用户鉴权**；依赖部署方网络隔离或前置网关
- 启用 OpenAI 兼容后端时，密钥只应来自环境变量，勿写入仓库或 L2 内容

将服务暴露到公网而不加鉴权 / TLS，视为错误配置，而非功能预期。
